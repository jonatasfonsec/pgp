spring:
  datasource:
    url: jdbc:postgresql://localhost:5433/pgp
    username: postgres
    password: postgres

jpa:
database-platform: org.hibernate.dialect.PostgreSQLDialect 	
hibernate:
      ddl-auto: update
      show-sql: true
server:
port: 0	  